# Core dependencies for Robotic Hand V2.0
# Compatible with Python 3.9+

# Hand tracking (PC only)
opencv-python==4.8.1.78
mediapipe==0.10.8

# Raspberry Pi hardware control
adafruit-circuitpython-servokit==1.3.8
adafruit-circuitpython-pca9685==3.4.9
adafruit-circuitpython-motor==3.4.9
RPi.GPIO==0.7.1  # Raspberry Pi only

# Web interface
nicegui==1.4.20

# Standard libraries (usually included, but just in case)
# These are typically built-in but listed for reference
# socket
# json
# threading
# time
# pathlib
